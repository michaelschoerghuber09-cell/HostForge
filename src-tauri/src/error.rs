//! Unified backend error type.
//!
//! A single error type for the backend keeps command signatures consistent
//! and makes errors serializable back to the frontend. Variants are added
//! alongside the features that need them.
