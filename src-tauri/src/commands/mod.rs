//! Tauri command handlers.
//!
//! Each submodule groups the `#[tauri::command]` functions for one feature
//! area. They are registered with the Tauri builder in `lib.rs` via
//! `invoke_handler`. Empty for now — commands are added as features land.
