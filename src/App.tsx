/**
 * Application root.
 *
 * Intentionally minimal — this is the project foundation only.
 * UI, routing, state and feature modules are added under `src/` later.
 */
function App() {
  return <div id="app">HostForge</div>;
}

export default App;
